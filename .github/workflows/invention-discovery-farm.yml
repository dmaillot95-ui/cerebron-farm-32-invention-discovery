name: CEREBRON Farm 32 Invention Discovery

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  models: read

jobs:
  agents:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        include:
          - {role: PROBLEM_DISCOVERY_SPECIALIST, model: openai/gpt-4o, focus: unmet needs bottlenecks contradictions}
          - {role: MECHANISM_INVENTOR, model: openai/gpt-4.1, focus: physical computational biological mechanisms}
          - {role: CROSS_DOMAIN_TRANSFER_SPECIALIST, model: openai/gpt-4o, focus: transfer mechanisms across domains}
          - {role: CONTRADICTION_MINER, model: openai/gpt-4.1, focus: contradictions anomalies unresolved tradeoffs}
          - {role: BIOMIMICRY_INVENTOR, model: openai/gpt-4o, focus: biological principles translated into engineering}
          - {role: ARCHITECTURE_SYNTHESIS_SPECIALIST, model: openai/gpt-4.1, focus: system architectures interfaces modularity}
          - {role: MATERIAL_PROCESS_INVENTOR, model: openai/gpt-4o, focus: materials processes manufacturing concepts}
          - {role: ALGORITHM_INVENTOR, model: openai/gpt-4.1, focus: algorithms optimization representations}
          - {role: SYSTEMS_INVENTOR, model: openai/gpt-4o, focus: integrated system inventions}
          - {role: SIMPLIFICATION_ENGINEER, model: openai/gpt-4.1, focus: remove complexity preserve capability}
          - {role: FEASIBILITY_AUDITOR, model: openai/gpt-4o, focus: physics engineering resource feasibility}
          - {role: MANUFACTURABILITY_AUDITOR, model: openai/gpt-4.1, focus: fabrication supply chain maintainability}
          - {role: VALUE_ECONOMICS_AUDITOR, model: openai/gpt-4o, focus: value cost adoption economic constraints}
          - {role: SAFETY_FAILURE_AUDITOR, model: openai/gpt-4.1, focus: hazards failure modes unintended consequences}
          - {role: EXPERIMENT_DESIGNER, model: openai/gpt-4o, focus: minimum decisive tests falsification}
          - {role: NOVELTY_PRIOR_ART_STYLE_AUDITOR, model: openai/gpt-4.1, focus: conceptual novelty collision risks without legal claims}
          - {role: DISCOVERY_HYPOTHESIS_GENERATOR, model: openai/gpt-4o, focus: testable scientific discovery hypotheses}
          - {role: RED_TEAM_INVENTION, model: openai/gpt-4.1, focus: destroy weak ideas expose hidden assumptions}
          - {role: INDEPENDENT_REPLICATOR, model: openai/gpt-4o, focus: independently reproduce reasoning and candidate ranking}
          - {role: OMEGA_INVENTION_DISCOVERY_JUDGE, model: openai/gpt-4.1, focus: evidence bounded synthesis unresolved unknowns}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Run real external model worker
        continue-on-error: true
        env:
          ROLE: ${{ matrix.role }}
          MODEL: ${{ matrix.model }}
          FOCUS: ${{ matrix.focus }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python worker/agent.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: farm32-${{ matrix.role }}
          path: results/*.json

  omega:
    needs: agents
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: farm32-*
          path: collected
          merge-multiple: true
      - name: Build Omega evidence packet
        run: |
          python - <<'PY'
          import json, glob, os
          rows=[]
          for p in glob.glob('collected/*.json'):
              try:
                  rows.append(json.load(open(p, encoding='utf-8')))
              except Exception as e:
                  rows.append({'file':p,'parse_error':repr(e),'status':'ARTIFACT_PARSE_FAILED'})
          successes=[x for x in rows if x.get('inference_success') is True and x.get('status')=='UNREVIEWED_EXTERNAL_AGENT_OUTPUT']
          failures=[x for x in rows if x not in successes]
          packet={
            'farm':32,
            'name':'Invention Discovery',
            'roles_expected':20,
            'roles_returned':len(rows),
            'successful_external_ai_calls':len(successes),
            'failed_external_ai_calls':len(failures),
            'all_external_calls_verified_successful':len(rows)==20 and len(successes)==20,
            'providers':sorted(set(x.get('provider','') for x in rows if x.get('provider'))),
            'model_families':sorted(set(x.get('model','') for x in rows if x.get('model'))),
            'rules':['CLAIM <= EVIDENCE','IDEA != INVENTION','PATENTABILITY != FTO','SIMULATION != TEST','UNKNOWN REMAINS UNKNOWN','VERIFY BEFORE COMMIT'],
            'results':rows
          }
          os.makedirs('omega',exist_ok=True)
          json.dump(packet,open('omega/farm32-omega.json','w',encoding='utf-8'),ensure_ascii=False,indent=2)
          print(json.dumps({k:v for k,v in packet.items() if k!='results'},indent=2))
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: invention-discovery-farm-32-omega-packet
          path: omega/farm32-omega.json
